syntax = "proto3";
package kratos.api;

option go_package = "github.com/J-Y-Zhang/light-cloud-disk/app/file/internal/conf;conf";

import "google/protobuf/duration.proto";

message Bootstrap {
  Server server = 1;
  Data data = 2;
  Upload upload = 3;
  Storage storage = 4;
}

message Server {
  message GRPC {
    string network = 1;
    string addr = 2;
    google.protobuf.Duration timeout = 3;
  }
  GRPC grpc = 1;
}

message Data {
  message Database {
    string driver = 1;
    int32 max_idle_conns = 2;
    int32 max_open_conns = 3;
    google.protobuf.Duration conn_max_lifetime = 4;
  }
  message Redis {
    google.protobuf.Duration read_timeout = 1;
    google.protobuf.Duration write_timeout = 2;
  }
  message Kafka {
    repeated string brokers = 1;
    string transfer_topic = 2;
    string thumbnail_topic = 3;
    string cloud_migrate_topic = 4;
  }
  Database database = 1;
  Redis redis = 2;
  Kafka kafka = 3;
}

message Upload {
  int64 chunk_size = 1;
  string temp_dir = 2;
  string store_dir = 3;
}

message Storage {
  // Local disk max bytes for chunk temp and merge temp
  int64 local_max_bytes = 1;

  message SeaweedFS {
    string endpoint = 1; // S3 API 端点，例如 http://seaweedfs:8333
    string region = 2; // 通常为 "us-east-1"
    string bucket = 3;
    string access_key = 4; // 优先读取环境变量 SEAWEEDFS_ACCESS_KEY
    string secret_key = 5; // 优先读取环境变量 SEAWEEDFS_SECRET_KEY
    int64 max_bytes = 6; // SeaweedFS 总容量上限
    int32 threshold_percent = 7; // 达到该百分比后触发淘汰
  }

  message OSS {
    string endpoint = 1;
    string bucket = 2;
    string access_key_id = 3; // 来自环境变量 OSS_ACCESS_KEY_ID
    string access_key_secret = 4; // 来自环境变量 OSS_ACCESS_KEY_SECRET
    string region = 5;
  }

  SeaweedFS seaweedfs = 2;
  OSS oss = 3;
}
